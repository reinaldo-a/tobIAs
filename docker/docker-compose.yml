<<<<<<< Updated upstream
services:
  db:
    image: postgres:16
    container_name: postgres_db
    restart: always
    env_file:
      - ../.env
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

  pgadmin:
    image: dpage/pgadmin4
    container_name: pgadmin
    restart: always
    environment:
      PGADMIN_DEFAULT_EMAIL: admin@admin.com
      PGADMIN_DEFAULT_PASSWORD: admin
    ports:
      - "5050:80"
    depends_on:
      - db

  app: # Este é o seu projeto Java com Tomcat
    build:
      context: ..
      dockerfile: docker/Dockerfile
    container_name: java_app
    ports:
      - "8888:8080"
    depends_on:
      - db
    env_file:
      - ../.env
    volumes:
      - ../pom.xml:/workspace/pom.xml:delegated
      - ../src/main/java:/workspace/src/main/java:delegated
      - ../src/main/resources:/workspace/src/main/resources:delegated
      - ../target:/workspace/target:delegated
      - ../src/main/webapp/assets:/usr/local/tomcat/webapps/ROOT/assets:delegated
      - ../src/main/webapp/WEB-INF/templates:/usr/local/tomcat/webapps/ROOT/WEB-INF/templates:delegated
      - ../src/main/webapp/WEB-INF/web.xml:/usr/local/tomcat/webapps/ROOT/WEB-INF/web.xml:delegated

volumes:
  postgres_data:
=======
  services:

    app:
      build:
        context: ..
        dockerfile: docker/Dockerfile
      container_name: java_app
      depends_on:
        - db
      volumes:
        - ../src:/app/src
      command: sh -c "find src -name '*.java' -exec javac {} + && java -cp src Main"

    db:
      image: postgres:16
      container_name: postgres_db
      restart: always
      environment:
        POSTGRES_DB: learn_db
        POSTGRES_USER: learn_user
        POSTGRES_PASSWORD: learn_pass
      ports:
        - "5433:5432"
      volumes:
        - postgres_data:/var/lib/postgresql/data

    pgadmin:
      image: dpage/pgadmin4
      container_name: pgadmin
      restart: always
      environment:
        PGADMIN_DEFAULT_EMAIL: admin@admin.com
        PGADMIN_DEFAULT_PASSWORD: admin
      ports:
        - "5050:80"
      depends_on:
        - db
      volumes:
        - pgadmin_data:/var/lib/pgadmin

  volumes:
    postgres_data:
    pgadmin_data:
>>>>>>> Stashed changes
